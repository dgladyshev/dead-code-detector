spring.application.name: dead-code-detector
spring.boot.admin.client.enabled: false
server.error.whitelabel.enabled: false

data.dir: ./data
scitools.dir: /Users/dgladyshev/scitools/bin/macosx #TODO fix

endpoints:
  health:
    sensitive: false
  restart:
    enabled: false
  shutdown:
    enabled: false

---
spring:
    profiles: dev

url: http://some_url

---
spring:
    profiles: stage

url: http://some_url
---

spring:
    profiles: prod

url: http://some_url